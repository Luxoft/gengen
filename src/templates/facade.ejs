export class Facade {
<% Object.entries(controllers).forEach(([controllerName, methods]) => { %>
    public static <%=controllerName%>Service = {
        <% methods.forEach((method) => { %>
            <%= method %>: "<%= controllerName %>/<%= method %>",
        <% }) %>
    }
<% }) %>
}